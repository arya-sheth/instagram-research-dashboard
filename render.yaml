services:
  - type: web
    name: instagram-research-api
    runtime: docker
    rootDir: apps/api
    plan: free
    dockerfilePath: Dockerfile
    dockerContext: ../..
    healthCheckPath: /analysis/catalog
    envVars:
      - key: NODE_VERSION
        value: 22
