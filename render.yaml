services:
  - type: web
    name: instagram-research-api
    runtime: node
    rootDir: apps/api
    plan: free
    buildCommand: npm install && npm run install:browsers && npm run build
    startCommand: npm run start
    healthCheckPath: /analysis/catalog
    envVars:
      - key: NODE_VERSION
        value: 22
